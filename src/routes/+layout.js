/**
 * Layout configuration for static site generation
 */

// This tells SvelteKit to prerender this route and all child routes
export const prerender = true;

// This tells SvelteKit to treat this as a SPA (optional)
export const ssr = false;
